database:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/database

server:
  type: simple
  applicationContextPath: /
  connector:
    type: http
    port: 8080

logging:
  appenders:
    - type: console